defmodule Remote.AddGatewayTest do
  @moduletag [:remote]
  use ExUnit.Case, async: true

  test "the truth" do
    assert 1 + 1 == 2
  end

  test "additional truth" do
    assert 1 + 1 == 2
  end
end
